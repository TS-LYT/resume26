# C++ 多态

## 编译运行

```bash
cmake -S . -B build
cmake --build build
./build/demo
```

## 面试口述

多态就是通过基类指针或引用调用虚函数，实际执行派生类的实现。它适合做统一接口，例如不同设备驱动都实现 `start()`、`stop()`、`read()`。

## 常见错误

1. 基类析构函数忘记写 `virtual`。
2. 函数签名写错，没有真正重写，建议加 `override`。
3. 不需要运行时多态的地方过度使用继承，增加复杂度。
