# C++ 类：对象生命周期

## 学什么

这个示例演示：

```text
构造函数
析构函数
拷贝构造函数
拷贝赋值运算符
this 指针
```

## 编译运行

```bash
cmake -S . -B build
cmake --build build
./build/demo
```

## 面试口述

构造函数负责初始化对象，析构函数负责释放对象持有的资源。对象被拷贝时会调用拷贝构造或拷贝赋值。如果类管理堆内存、文件描述符、锁等资源，就要特别注意拷贝语义。
