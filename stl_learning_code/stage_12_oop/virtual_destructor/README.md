# C++ 类：虚析构和接口类

## 学什么

这个示例演示为什么基类析构函数要写成 `virtual`。

当你通过基类指针删除派生类对象时：

```cpp
Device* p = new UartDevice;
delete p;
```

如果 `Device` 的析构函数不是虚函数，派生类析构函数可能不会正确执行。

## 面试口述

只要一个类准备作为多态基类使用，也就是有虚函数并可能通过基类指针删除对象，它的析构函数就应该是虚析构函数。
