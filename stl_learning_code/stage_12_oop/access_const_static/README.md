# C++ 类：访问权限、const、static

## 学什么

```text
public    外部可以访问
private   只有类内部能访问
protected 类内部和子类能访问
const 成员函数 不修改对象状态
static 成员 属于类，不属于某一个对象
```

## 编译运行

```bash
cmake -S . -B build
cmake --build build
./build/demo
```
