# 进程通信 IPC

## 常见 IPC 方式

```text
pipe            匿名管道，常用于父子进程
FIFO            有名管道，无亲缘关系进程也能通信
shared memory   共享内存，速度快，但需要同步机制
message queue   消息队列，按消息传递
socket          可用于本机或跨机器通信
signal          信号，适合简单事件通知
```

## 面试口述

管道适合简单字节流通信；共享内存速度快，但要自己处理互斥同步；socket 最通用，既能本机通信，也能网络通信。
