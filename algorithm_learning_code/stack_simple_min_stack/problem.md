# 栈简单：最小栈

## 题目

设计一个栈，支持 `push`、`pop`、`top`，并能在 `O(1)` 时间获取当前最小值。

## 思路

使用两个栈：

1. 数据栈保存所有元素。
2. 最小栈保存“到当前位置为止的最小值”。
3. 每次入栈时，最小栈也入一个当前最小值。
4. 出栈时，两个栈一起出。

## 复杂度

每个操作时间复杂度：`O(1)`  
空间复杂度：`O(n)`

## 面试口述

最小栈用辅助栈记录历史最小值。这样栈顶元素弹出后，还能立刻知道新的最小值。
