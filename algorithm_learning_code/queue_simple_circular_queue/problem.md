# 队列简单：循环队列

## 题目

用数组实现一个固定容量的循环队列，支持：

```text
push 入队
pop 出队
front 查看队头
empty 判断空
full 判断满
```

## 思路

普通数组队列如果一直 `pop`，前面空间会浪费。循环队列让下标到末尾后回到开头。

核心变量：

```text
front: 队头下标
rear : 下一个写入位置
size : 当前元素个数
```

## 复杂度

每个操作都是 `O(1)`。

## 面试口述

循环队列适合固定容量缓存。用取模运算让下标循环移动，避免频繁搬移数组元素。
