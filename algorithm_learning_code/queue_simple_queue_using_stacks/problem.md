# 队列简单：用两个栈实现队列

## 题目

使用两个栈实现队列，支持 `push`、`pop`、`empty`。

## 思路

一个栈 `in` 负责入队，一个栈 `out` 负责出队：

1. `push` 时压入 `in`。
2. `pop` 时如果 `out` 为空，就把 `in` 全部倒入 `out`。
3. 再从 `out` 弹出，顺序就是队列的先进先出。

## 复杂度

单次最坏 `O(n)`，但均摊 `O(1)`。

## 面试口述

两个栈可以实现队列。元素从入栈倒到出栈时顺序会反转一次，刚好恢复成先进先出。
