# 数组中等：旋转数组

## 题目

给定数组，把数组向右旋转 `k` 步。

示例：

```text
nums = [1,2,3,4,5,6,7], k = 3
输出 = [5,6,7,1,2,3,4]
```

## 思路

使用三次反转：

1. 反转整个数组。
2. 反转前 `k` 个元素。
3. 反转剩余元素。

## 复杂度

时间复杂度：`O(n)`  
空间复杂度：`O(1)`

## 面试口述

右旋 `k` 位可以用三次反转原地完成。先整体反转，再分别反转前 `k` 段和后半段。
