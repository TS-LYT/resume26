# 链表简单：删除指定值节点

## 题目

给定单链表头节点和一个值 `val`，删除所有值等于 `val` 的节点，返回新的头节点。

## 思路

使用哑节点 `dummy`：

1. `dummy.next = head`，这样删除头节点也能统一处理。
2. `prev` 指向当前节点的前一个节点。
3. 如果 `prev->next` 需要删除，就跳过它。
4. 否则 `prev` 前进。

## 复杂度

时间复杂度：`O(n)`  
空间复杂度：`O(1)`

## 面试口述

链表删除题常用哑节点，能避免头节点被删除时单独写一套逻辑。
